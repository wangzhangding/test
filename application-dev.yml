server:
  port: ${port:8762}
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: eruak-test
  data:
    mongodb:
      database: log
      port: 27017